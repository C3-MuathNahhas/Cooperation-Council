import React, { useState } from "react";
import { useHistory, useRouteMatch } from "react-router-dom";
import axios from "axios";
import "./Home.css";

export const MyBooking = ({ setvalue, setadult }) => {
  const click = () => {};

  return <div></div>;
};
